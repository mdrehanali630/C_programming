#include <stdio.h>
 void main()
  {
   int a;
   printf("Enter the value of a \n");
   scanf("%d",&a);
   if(a%2==0)
   { 
    printf("%d is even number",a);
    }
    printf("%d is odd number",a);
    }
